# Dependencies
node_modules/

# Build output
dist/

# Environment
.env
.env.local

# Credentials
.mcpregistry_*

# Logs
*.log

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Entire CLI (session logs and local data)
.entire/
.githooks/*.backup

# Worktrees
.worktrees/

# Coverage
coverage/

# Claude Code (local settings ignored, but commands/ and settings are tracked)
.claude/*
!.claude/commands/
!.claude/settings.json
.beads/
issues.jsonl
.replicant/

# MCPB bundle — a 60MB binary rebuilt by scripts/build-bundle.sh on every
# release and attached to the GitHub Release. Committing it grew the repo by
# the full bundle size each release.
replicant-mcp.mcpb
*.mcpb
.mcpb-build/
