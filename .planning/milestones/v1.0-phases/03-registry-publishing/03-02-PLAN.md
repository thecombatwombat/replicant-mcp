---
phase: 03-registry-publishing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PUB-02, PUB-03, PUB-04]

must_haves:
  truths:
    - "Smithery listing submitted for replicant-mcp"
    - "Glama listing confirmed live and claimed"
    - "Cursor marketplace submission completed"
  artifacts:
    - path: "smithery.ai"
      provides: "replicant-mcp listing (pending or live)"
    - path: "glama.ai/mcp/servers/thecombatwombat/replicant-mcp"
      provides: "Confirmed live server listing with tools"
    - path: "cursor.com/marketplace"
      provides: "replicant-mcp plugin submission (pending review)"
  key_links:
    - from: "smithery.yaml (on master)"
      to: "smithery.ai listing"
      via: "GitHub repo connection at smithery.ai/new"
      pattern: "replicant-mcp"
    - from: "glama.json (on master)"
      to: "glama.ai listing"
      via: "Auto-indexing (already complete)"
      pattern: "thecombatwombat/replicant-mcp"
    - from: ".cursor-plugin/plugin.json (on master)"
      to: "cursor.com/marketplace"
      via: "GitHub repo URL submission"
      pattern: "replicant-mcp"
---

<objective>
Submit replicant-mcp to Smithery and Cursor marketplaces, and confirm Glama listing is live.

Purpose: Smithery and Cursor require web-based submissions using the config files already on master. Glama auto-indexed during Phase 1 and just needs confirmation. Together with Plan 01 (MCP Registry), this completes all four registry publishing requirements.

Output: Smithery and Cursor submissions completed, Glama listing confirmed.
</objective>

<execution_context>
@/Users/architjoshi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/architjoshi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-registry-publishing/03-RESEARCH.md
@.planning/phases/01-config-files-and-pull-requests/01-02-SUMMARY.md

<interfaces>
<!-- Config files already on master that marketplaces will read -->

smithery.yaml (created in Phase 1, Plan 02, PR #101 merged):
- startCommand.type: "stdio"
- startCommand.configSchema: optional projectRoot string
- startCommand.commandFunction: uses npx replicant-mcp@latest

glama.json (created in Phase 1, Plan 02, PR #102 merged):
- $schema: "https://glama.ai/schemas/glama-server-config.schema.json"
- maintainers: ["thecombatwombat"]

.cursor-plugin/plugin.json (created in Phase 1, Plan 03):
- Plugin manifest with name, description, tools list

GitHub repo: https://github.com/thecombatwombat/replicant-mcp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Glama listing is live (PUB-03)</name>
  <action>
    PUB-03 was identified as already complete during research. Glama auto-indexed replicant-mcp when glama.json was merged to master. Verify the listing is still live:

    ```bash
    # Check HTTP status
    curl -sL "https://glama.ai/mcp/servers/thecombatwombat/replicant-mcp" -o /dev/null -w "%{http_code}"
    # Expected: 200
    ```

    If the listing returns 200, PUB-03 is confirmed complete. No further action needed.

    If it returns 404 or other error: fall back to visiting glama.ai, signing in with GitHub (thecombatwombat), and claiming ownership manually. But research confirmed this is unnecessary -- the listing is already live and claimed.
  </action>
  <verify>
    <automated>curl -sL "https://glama.ai/mcp/servers/thecombatwombat/replicant-mcp" -o /dev/null -w "%{http_code}" | grep -q "200"</automated>
  </verify>
  <done>Glama listing returns HTTP 200 at glama.ai/mcp/servers/thecombatwombat/replicant-mcp</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Submit to Smithery and Cursor marketplaces (PUB-02, PUB-04)</name>
  <decision>Both Smithery and Cursor require web browser submissions that Claude cannot perform. Config files are already on master -- the marketplaces read them from the GitHub repo.</decision>
  <context>
    **Smithery (PUB-02):**
    - smithery.yaml is on master with stdio transport config
    - Smithery reads the config from the GitHub repo

    **Cursor (PUB-04):**
    - .cursor-plugin/plugin.json is on master
    - All Cursor plugins are manually reviewed before listing (no SLA)
  </context>
  <action>
    **Smithery submission:**
    1. Go to https://smithery.ai/new
    2. Sign in with GitHub (thecombatwombat account)
    3. Enter repo URL: `https://github.com/thecombatwombat/replicant-mcp`
    4. Smithery will read `smithery.yaml` from master and auto-configure
    5. Review the listing preview
    6. Submit

    If smithery.ai/new only shows an HTTP URL option (no GitHub repo option): fall back to CLI:
    ```bash
    npx @smithery/cli auth login
    npx @smithery/cli mcp publish https://github.com/thecombatwombat/replicant-mcp -n @thecombatwombat/replicant-mcp
    ```

    **Cursor submission:**
    1. Go to https://cursor.com/marketplace/publish
    2. Enter repo URL: `https://github.com/thecombatwombat/replicant-mcp`
    3. Cursor reads `.cursor-plugin/plugin.json` from the repo
    4. Review the listing preview
    5. Submit for review

    Note: Cursor plugins are manually reviewed. Submission does not mean immediate listing.
  </action>
  <resume-signal>Type "submitted" after completing both Smithery and Cursor submissions (or note which ones succeeded/failed)</resume-signal>
</task>

</tasks>

<verification>
1. Glama: `curl -sL "https://glama.ai/mcp/servers/thecombatwombat/replicant-mcp" -o /dev/null -w "%{http_code}"` returns 200
2. Smithery: Search "replicant-mcp" at smithery.ai shows listing (may take a few minutes to appear)
3. Cursor: Submission completed at cursor.com/marketplace/publish (listing pending review)
</verification>

<success_criteria>
- Glama listing confirmed live at expected URL (HTTP 200)
- Smithery submission completed (listing visible or pending)
- Cursor submission completed (pending review is acceptable -- PUB-04 requires "submitted," not "listed")
</success_criteria>

<output>
After completion, create `.planning/phases/03-registry-publishing/03-02-SUMMARY.md`
</output>
