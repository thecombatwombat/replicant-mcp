---
phase: 01-config-files-and-pull-requests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .mcp/server.json
  - package.json
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - PR-01

must_haves:
  truths:
    - ".mcp/server.json exists with valid MCP Registry schema and correct metadata"
    - "package.json contains mcpName field matching server.json name exactly"
    - "PR is open on branch chore/mcp-registry-listing with both file changes"
  artifacts:
    - path: ".mcp/server.json"
      provides: "MCP Registry server manifest"
      contains: "io.github.thecombatwombat/replicant-mcp"
    - path: "package.json"
      provides: "mcpName field for registry"
      contains: "mcpName"
  key_links:
    - from: ".mcp/server.json"
      to: "package.json"
      via: "mcpName must match server.json name field"
      pattern: "io\\.github\\.thecombatwombat/replicant-mcp"
---

<objective>
Create MCP Registry config files and open PR for replicant-mcp registry listing.

Purpose: MCP Registry is the protocol-level standard registry consumed by VS Code, Glama, and other clients. This is the canonical listing.
Output: PR open on `chore/mcp-registry-listing` with `.mcp/server.json` and updated `package.json`.
</objective>

<execution_context>
@/Users/architjoshi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/architjoshi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-13-marketplace-mcp-registry.md
@.planning/phases/01-config-files-and-pull-requests/01-RESEARCH.md

<interfaces>
<!-- Current package.json top-level fields (executor needs to know where to add mcpName) -->
From package.json (first 8 lines):
```json
{
  "name": "replicant-mcp",
  "version": "1.6.0",
  "description": "Android MCP server for AI-assisted Android development",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "replicant-mcp": "dist/index.js"
```

mcpName should be added after "description" or near top-level metadata fields.

<!-- .gitignore note: .mcp/ directory is NOT gitignored. Only .mcp.json is. So .mcp/server.json will track fine. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MCP Registry config files</name>
  <files>.mcp/server.json, package.json</files>
  <action>
Create a worktree and branch for MCP Registry work:

```bash
git worktree add .worktrees/mcp-registry chore/mcp-registry-listing
```

If the branch already exists, clean it up first (`git branch -D chore/mcp-registry-listing`). If the worktree path already exists, remove it first (`git worktree remove .worktrees/mcp-registry`).

In the worktree directory `.worktrees/mcp-registry/`:

1. Create `.mcp/` directory and `.mcp/server.json` with this exact content:
```json
{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.thecombatwombat/replicant-mcp",
  "title": "replicant-mcp",
  "description": "Android MCP server for AI-assisted development. Build, test, emulate, and automate.",
  "repository": {
    "url": "https://github.com/thecombatwombat/replicant-mcp",
    "source": "github"
  },
  "version": "1.6.0",
  "packages": [
    {
      "registryType": "npm",
      "registryBaseUrl": "https://registry.npmjs.org",
      "identifier": "replicant-mcp",
      "version": "1.6.0",
      "runtimeHint": "npx",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": []
    }
  ]
}
```

IMPORTANT: Description is 83 characters (schema max is 100). Per research, the plan doc description of 161 chars is too long. Use the shortened version above.

2. Add `mcpName` field to `package.json`. Add it at the top level, after "description":
```json
"mcpName": "io.github.thecombatwombat/replicant-mcp"
```

The mcpName value MUST exactly match the `name` field in server.json.
  </action>
  <verify>
    <automated>cd .worktrees/mcp-registry && jq . .mcp/server.json && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const s=JSON.parse(require('fs').readFileSync('.mcp/server.json','utf8')); console.assert(p.mcpName===s.name, 'mcpName mismatch'); console.assert(s.description.length<=100, 'description too long: '+s.description.length); console.log('OK: mcpName matches, description '+s.description.length+' chars')"</automated>
  </verify>
  <done>`.mcp/server.json` is valid JSON with description under 100 chars. `package.json` has `mcpName` matching server.json `name` exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Commit and open MCP Registry PR</name>
  <files>.mcp/server.json, package.json</files>
  <action>
In the worktree directory `.worktrees/mcp-registry/`:

1. Run the existing test suite to confirm no regressions from the package.json edit:
```bash
npm run test:coverage
```

2. Stage and commit:
```bash
git add .mcp/server.json package.json
git commit -m "chore: add MCP Registry server.json and mcpName for registry publishing"
```

3. Push the branch:
```bash
git push origin chore/mcp-registry-listing
```

4. Create PR using `gh pr create`:
- Title: "chore: add MCP Registry config for marketplace listing"
- Body should mention:
  - Adds `.mcp/server.json` with MCP Registry schema
  - Adds `mcpName` to package.json for registry namespace
  - Part of marketplace distribution effort
  - No runtime changes -- config files only

5. Verify PR is created and CI is triggered.

IMPORTANT: Per CLAUDE.md -- never push directly to master. This creates a branch + PR as required.
  </action>
  <verify>
    <automated>gh pr view chore/mcp-registry-listing --json title,state,files 2>&1</automated>
  </verify>
  <done>PR is open on `chore/mcp-registry-listing` branch. CI is triggered. PR contains exactly `.mcp/server.json` and `package.json` changes.</done>
</task>

</tasks>

<verification>
- `.mcp/server.json` is valid JSON matching MCP Registry schema
- `description` field is <= 100 characters
- `mcpName` in `package.json` matches `name` in `server.json` exactly
- `version` fields are set to `1.6.0` per user decision
- PR is open and CI passes
- `npm run test:coverage` shows no regressions
</verification>

<success_criteria>
PR open on `chore/mcp-registry-listing` with valid `.mcp/server.json` (83-char description, v1.6.0) and `mcpName` added to `package.json`. Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-files-and-pull-requests/01-01-SUMMARY.md`
</output>
