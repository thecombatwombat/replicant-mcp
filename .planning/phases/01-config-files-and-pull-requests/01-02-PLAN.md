---
phase: 01-config-files-and-pull-requests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - smithery.yaml
  - glama.json
autonomous: true
requirements:
  - CFG-03
  - CFG-04
  - PR-02
  - PR-03

must_haves:
  truths:
    - "smithery.yaml exists with valid YAML, stdio config, commandFunction, and projectRoot schema"
    - "glama.json exists with valid JSON, schema URL, and thecombatwombat maintainer"
    - "PR is open on chore/smithery-listing with smithery.yaml"
    - "PR is open on chore/glama-listing with glama.json"
  artifacts:
    - path: "smithery.yaml"
      provides: "Smithery marketplace configuration"
      contains: "commandFunction"
    - path: "glama.json"
      provides: "Glama marketplace ownership claim"
      contains: "thecombatwombat"
  key_links:
    - from: "smithery.yaml"
      to: "npm:replicant-mcp"
      via: "commandFunction references replicant-mcp@latest"
      pattern: "replicant-mcp@latest"
---

<objective>
Create Smithery and Glama config files and open PRs for both marketplace listings.

Purpose: Smithery is one of the largest MCP directories (~7k servers). Glama auto-indexes repos but needs `glama.json` for ownership claim. Both are single-file additions with no overlap.
Output: Two PRs open -- `chore/smithery-listing` with `smithery.yaml` and `chore/glama-listing` with `glama.json`.
</objective>

<execution_context>
@/Users/architjoshi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/architjoshi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-13-marketplace-smithery.md
@docs/plans/2026-03-13-marketplace-glama.md
@.planning/phases/01-config-files-and-pull-requests/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Smithery config, commit, and open PR</name>
  <files>smithery.yaml</files>
  <action>
Create a worktree and branch for Smithery:

```bash
git worktree add .worktrees/smithery chore/smithery-listing
```

If branch or worktree path already exists, clean up first.

In `.worktrees/smithery/`, create `smithery.yaml` at repo root with this exact content:

```yaml
# Smithery configuration: https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties:
      projectRoot:
        type: string
        description: "Absolute path to the Android project root (optional, for Gradle builds)"
  commandFunction:
    |-
    (config) => ({
      command: 'npx',
      args: ['-y', 'replicant-mcp@latest'],
      env: config.projectRoot ? { REPLICANT_PROJECT_ROOT: config.projectRoot } : {}
    })
```

Key details:
- Uses `@latest` per user decision (not a pinned version)
- `configSchema` exposes optional `projectRoot` for Gradle builds
- `commandFunction` is a JS arrow function that Smithery evaluates at install time

Stage, commit, push, and create PR:
```bash
git add smithery.yaml
git commit -m "chore: add smithery.yaml for Smithery marketplace listing"
git push origin chore/smithery-listing
```

Create PR with `gh pr create`:
- Title: "chore: add Smithery marketplace config"
- Body: Adds `smithery.yaml` for Smithery listing. Configures stdio transport with optional projectRoot for Gradle builds. Config files only, no runtime changes.
  </action>
  <verify>
    <automated>cd .worktrees/smithery && node -e "const yaml=require('yaml'); const doc=yaml.parse(require('fs').readFileSync('smithery.yaml','utf8')); console.assert(doc.startCommand.type==='stdio', 'not stdio'); console.assert(doc.startCommand.commandFunction.includes('replicant-mcp@latest'), 'missing @latest'); console.log('OK: valid smithery.yaml')" && gh pr view chore/smithery-listing --json title,state 2>&1</automated>
  </verify>
  <done>`smithery.yaml` is valid YAML with stdio transport and `replicant-mcp@latest` in commandFunction. PR is open on `chore/smithery-listing`.</done>
</task>

<task type="auto">
  <name>Task 2: Create Glama config, commit, and open PR</name>
  <files>glama.json</files>
  <action>
Create a worktree and branch for Glama:

```bash
git worktree add .worktrees/glama chore/glama-listing
```

If branch or worktree path already exists, clean up first.

In `.worktrees/glama/`, create `glama.json` at repo root with this exact content:

```json
{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": ["thecombatwombat"]
}
```

This is the minimum required content. Glama auto-indexes the repo and extracts tool information from the codebase. The `glama.json` file only claims maintainer ownership.

Stage, commit, push, and create PR:
```bash
git add glama.json
git commit -m "chore: add glama.json for Glama marketplace listing"
git push origin chore/glama-listing
```

Create PR with `gh pr create`:
- Title: "chore: add Glama marketplace config"
- Body: Adds `glama.json` for Glama maintainer ownership claim. Glama auto-indexes public repos; this file lets us claim and manage the listing. Config file only, no runtime changes.
  </action>
  <verify>
    <automated>cd .worktrees/glama && jq . glama.json && node -e "const g=JSON.parse(require('fs').readFileSync('glama.json','utf8')); console.assert(g.maintainers.includes('thecombatwombat'), 'missing maintainer'); console.log('OK: valid glama.json')" && gh pr view chore/glama-listing --json title,state 2>&1</automated>
  </verify>
  <done>`glama.json` is valid JSON with thecombatwombat as maintainer. PR is open on `chore/glama-listing`.</done>
</task>

</tasks>

<verification>
- `smithery.yaml` is valid YAML with stdio type, commandFunction using `replicant-mcp@latest`, and projectRoot config schema
- `glama.json` is valid JSON with schema URL and thecombatwombat maintainer
- Both PRs are open and CI passes
- Each branch contains exactly one new file (no existing files modified)
</verification>

<success_criteria>
Two PRs open: `chore/smithery-listing` with valid `smithery.yaml` and `chore/glama-listing` with valid `glama.json`. Neither PR modifies existing files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-files-and-pull-requests/01-02-SUMMARY.md`
</output>
