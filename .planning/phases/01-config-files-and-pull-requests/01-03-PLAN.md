---
phase: 01-config-files-and-pull-requests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .cursor-plugin/plugin.json
  - .mcp.json
  - .gitignore
autonomous: true
requirements:
  - CFG-05
  - CFG-06
  - PR-04

must_haves:
  truths:
    - ".cursor-plugin/plugin.json exists with valid plugin manifest"
    - ".mcp.json exists with valid MCP server config and is NOT gitignored"
    - ".gitignore no longer ignores .mcp.json"
    - "PR is open on chore/cursor-marketplace with all three file changes"
  artifacts:
    - path: ".cursor-plugin/plugin.json"
      provides: "Cursor plugin manifest"
      contains: "replicant-mcp"
    - path: ".mcp.json"
      provides: "MCP server config for Cursor"
      contains: "replicant-mcp@latest"
    - path: ".gitignore"
      provides: "Updated gitignore without .mcp.json entry"
  key_links:
    - from: ".cursor-plugin/plugin.json"
      to: ".mcp.json"
      via: "mcpServers field references .mcp.json"
      pattern: "mcpServers.*\\.mcp\\.json"
    - from: ".mcp.json"
      to: "npm:replicant-mcp"
      via: "command runs npx replicant-mcp@latest"
      pattern: "replicant-mcp@latest"

---

<objective>
Create Cursor marketplace plugin files and open PR for Cursor marketplace listing.

Purpose: Cursor marketplace lets IDE users discover and install MCP servers directly. Requires plugin manifest + MCP config + gitignore fix.
Output: PR open on `chore/cursor-marketplace` with `.cursor-plugin/plugin.json`, `.mcp.json`, and `.gitignore` modification.
</objective>

<execution_context>
@/Users/architjoshi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/architjoshi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-13-marketplace-cursor.md
@.planning/phases/01-config-files-and-pull-requests/01-RESEARCH.md

<interfaces>
<!-- CRITICAL: .mcp.json is currently in .gitignore (last line). Must remove it on this branch. -->
Current .gitignore relevant section (last 4 lines):
```
.beads/
.replicant/
.mcp.json
```

The `.mcp.json` line must be removed so the file can be tracked. This change is ONLY on the cursor branch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cursor plugin files and fix gitignore</name>
  <files>.cursor-plugin/plugin.json, .mcp.json, .gitignore</files>
  <action>
Create a worktree and branch for Cursor:

```bash
git worktree add .worktrees/cursor chore/cursor-marketplace
```

If branch or worktree path already exists, clean up first.

In `.worktrees/cursor/`:

1. Create `.cursor-plugin/` directory and `.cursor-plugin/plugin.json` with this exact content:
```json
{
  "name": "replicant-mcp",
  "description": "Android MCP server for AI-assisted Android development. Build APKs, launch emulators, manage devices, automate UI, and analyze logs.",
  "version": "1.6.0",
  "author": {
    "name": "Archit Joshi"
  },
  "homepage": "https://github.com/thecombatwombat/replicant-mcp",
  "repository": "https://github.com/thecombatwombat/replicant-mcp",
  "license": "MIT",
  "keywords": [
    "android",
    "mcp",
    "adb",
    "gradle",
    "emulator",
    "ui-automation",
    "mobile"
  ],
  "mcpServers": ".mcp.json"
}
```

2. Create `.mcp.json` at repo root with this exact content:
```json
{
  "mcpServers": {
    "replicant-mcp": {
      "command": "npx",
      "args": ["-y", "replicant-mcp@latest"],
      "env": {}
    }
  }
}
```

Uses `@latest` per user decision.

3. CRITICAL: Remove `.mcp.json` from `.gitignore`. Edit `.gitignore` in the worktree to remove the line `.mcp.json` (currently the last line of the file). Keep all other entries unchanged. This is necessary because `.mcp.json` is currently gitignored and `git add .mcp.json` would silently skip it.

After removing from gitignore, verify `.mcp.json` is no longer ignored:
```bash
git check-ignore .mcp.json
# Should return exit code 1 (not ignored)
```
  </action>
  <verify>
    <automated>cd .worktrees/cursor && jq . .cursor-plugin/plugin.json && jq . .mcp.json && git check-ignore .mcp.json; test $? -eq 1 && echo "OK: .mcp.json is NOT gitignored" && node -e "const p=JSON.parse(require('fs').readFileSync('.cursor-plugin/plugin.json','utf8')); console.assert(p.mcpServers==='.mcp.json', 'mcpServers link missing'); const m=JSON.parse(require('fs').readFileSync('.mcp.json','utf8')); console.assert(m.mcpServers['replicant-mcp'].args.includes('replicant-mcp@latest'), 'missing @latest'); console.log('OK: all Cursor files valid')"</automated>
  </verify>
  <done>`.cursor-plugin/plugin.json` is valid JSON with mcpServers pointing to `.mcp.json`. `.mcp.json` is valid JSON with `replicant-mcp@latest`. `.mcp.json` is NOT gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Commit and open Cursor marketplace PR</name>
  <files>.cursor-plugin/plugin.json, .mcp.json, .gitignore</files>
  <action>
In the worktree directory `.worktrees/cursor/`:

1. Stage all three files:
```bash
git add .cursor-plugin/plugin.json .mcp.json .gitignore
```

2. Verify staged files are correct (should show 2 new files + 1 modified):
```bash
git diff --cached --stat
```

3. Commit:
```bash
git commit -m "chore: add Cursor marketplace plugin manifest and MCP config"
```

4. Push the branch:
```bash
git push origin chore/cursor-marketplace
```

5. Create PR using `gh pr create`:
- Title: "chore: add Cursor marketplace plugin config"
- Body should mention:
  - Adds `.cursor-plugin/plugin.json` plugin manifest for Cursor marketplace
  - Adds `.mcp.json` with MCP server config (npx replicant-mcp@latest)
  - Removes `.mcp.json` from `.gitignore` so the file can be tracked (needed for Cursor plugin discovery)
  - Part of marketplace distribution effort
  - No runtime changes

IMPORTANT: The `.gitignore` change means `.mcp.json` will be tracked going forward. This is intentional -- the file serves as both a Cursor plugin component AND a valid MCP client config.
  </action>
  <verify>
    <automated>gh pr view chore/cursor-marketplace --json title,state,files 2>&1</automated>
  </verify>
  <done>PR is open on `chore/cursor-marketplace` branch. PR contains `.cursor-plugin/plugin.json`, `.mcp.json`, and `.gitignore` changes. CI is triggered.</done>
</task>

</tasks>

<verification>
- `.cursor-plugin/plugin.json` is valid JSON with name, description, mcpServers reference
- `.mcp.json` is valid JSON with replicant-mcp server config using `@latest`
- `.mcp.json` is NOT in `.gitignore` (on this branch)
- PR is open with exactly 3 files changed
- `npm run test:coverage` passes (no regressions from gitignore change)
</verification>

<success_criteria>
PR open on `chore/cursor-marketplace` with valid `.cursor-plugin/plugin.json`, `.mcp.json`, and `.gitignore` fix. `.mcp.json` is successfully tracked (not gitignored).
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-files-and-pull-requests/01-03-SUMMARY.md`
</output>
