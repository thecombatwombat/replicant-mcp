---
phase: 04-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/04-verification/04-01-SUMMARY.md
autonomous: false
requirements:
  - VER-01
  - VER-02
  - VER-03
  - VER-04
  - VER-05
  - VER-06
  - VER-07

must_haves:
  truths:
    - "MCP Registry API returns replicant-mcp listing with correct name, version, and active status"
    - "Glama listing returns HTTP 200 at expected URL"
    - "Smithery listing is visible in browser at smithery.ai (confirmed by user)"
    - "awesome-mcp-servers PR #3919 status is known (merged or pending)"
    - "MCPB Desktop Extensions form submission is confirmed via Phase 2 evidence"
    - "VER-06 is marked N/A to match FORM-02 deferral"
    - "Cursor marketplace submission is confirmed via Phase 3 evidence"
    - "REQUIREMENTS.md is updated with final verification status for all VER items"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated VER-01 through VER-07 status"
      contains: "VER-01"
  key_links:
    - from: "Phase 2 SUMMARY (02-01-SUMMARY.md)"
      to: "VER-05 status"
      via: "Document evidence of MCPB form submission"
      pattern: "MCPB.*submitted"
    - from: "Phase 3 SUMMARY (03-02-SUMMARY.md)"
      to: "VER-07 status"
      via: "Document evidence of Cursor submission"
      pattern: "Cursor.*submitted"
    - from: "MCP Registry API"
      to: "VER-01 status"
      via: "curl query to registry.modelcontextprotocol.io"
      pattern: "replicant-mcp.*active"
---

<objective>
Verify all 7 marketplace listings from Phases 1-3 are live or confirmed submitted.

Purpose: Final phase of marketplace distribution -- confirm every listing is reachable by developers.
Output: Verification results for all VER requirements, updated REQUIREMENTS.md with final status, and SUMMARY.md documenting the complete distribution outcome.
</objective>

<execution_context>
@/Users/architjoshi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/architjoshi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-verification/04-RESEARCH.md

@.planning/phases/02-form-submissions/02-01-SUMMARY.md
@.planning/phases/03-registry-publishing/03-01-SUMMARY.md
@.planning/phases/03-registry-publishing/03-02-SUMMARY.md
@.planning/phases/03-registry-publishing/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks and compile evidence for all 7 VER requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Execute all verifiable checks and compile evidence for every VER requirement. Work through the following in order:

**VER-01 (MCP Registry) -- Automated API check:**
Run: `curl -s "https://registry.modelcontextprotocol.io/v0.1/servers?search=replicant-mcp" | jq '{ name: .servers[0].server.name, version: .servers[0].server.version, description: .servers[0].server.description, npm_package: .servers[0].server.packages[0].identifier, transport: .servers[0].server.packages[0].transport.type, status: .servers[0]._meta["io.modelcontextprotocol.registry/official"].status, count: .metadata.count }'`
Expected: name=`io.github.thecombatwombat/replicant-mcp`, transport=`stdio`, status=`active`.
Cross-check with: `npm view replicant-mcp version` and `npm view replicant-mcp mcpName`.
Record PASS if name/status/transport match. Note version if stale vs npm (not a failure, just a note).

**VER-03 (Glama) -- Automated HTTP check:**
Run: `curl -sL "https://glama.ai/mcp/servers/thecombatwombat/replicant-mcp" -o /dev/null -w "%{http_code}"`
Expected: HTTP 200.
Record PASS if 200. FAIL if non-200.

**VER-05 (Anthropic MCPB) -- Document evidence:**
Read `.planning/phases/02-form-submissions/02-01-SUMMARY.md`. Confirm it documents the MCPB Desktop Extensions form submission.
Record CONFIRMED SUBMITTED. Note that no status tracking exists per Anthropic FAQ -- review timing is external.

**VER-06 (Claude Code Plugin Directory) -- Mark as deferred:**
FORM-02 was deferred to v2 during Phase 2 (requires building an actual Claude Code plugin, not just a form). Mark VER-06 as "N/A -- deferred per FORM-02 deferral to v2".

**VER-07 (Cursor) -- Document evidence:**
Read `.planning/phases/03-registry-publishing/03-02-SUMMARY.md`. Confirm it documents the Cursor marketplace submission.
Record CONFIRMED SUBMITTED. Note that Cursor has no review dashboard -- listing pending external review.

**Update REQUIREMENTS.md:**
Update the status of VER-01 through VER-07 in the requirements traceability table:
- VER-01: Check as complete if PASS
- VER-02: Leave unchecked (pending human verification in Task 2)
- VER-03: Check as complete if PASS
- VER-04: Leave unchecked (pending human verification in Task 2)
- VER-05: Check as complete with note "form submitted, review external"
- VER-06: Mark with strikethrough and "Deferred to v2" note (same format as FORM-02)
- VER-07: Check as complete with note "submitted, review external"

Also update the VER-06 line item to match the FORM-02 deferral pattern:
`- [ ] ~~**VER-06**: Claude Code Plugin Directory form submitted~~ -- Deferred (FORM-02 deferred to v2)`

Build a verification results table (to be included in SUMMARY later):

| VER ID | Marketplace | Status | Evidence |
|--------|-------------|--------|----------|
| VER-01 | MCP Registry | {result} | {API response details} |
| VER-02 | Smithery | PENDING HUMAN | Awaiting browser check |
| VER-03 | Glama | {result} | HTTP {code} |
| VER-04 | awesome-mcp-servers | PENDING HUMAN | Awaiting browser check |
| VER-05 | Anthropic MCPB | CONFIRMED SUBMITTED | Phase 2 summary evidence |
| VER-06 | Claude Code Plugin Dir | N/A (DEFERRED) | FORM-02 deferred to v2 |
| VER-07 | Cursor Marketplace | CONFIRMED SUBMITTED | Phase 3 summary evidence |
  </action>
  <verify>
    <automated>curl -s "https://registry.modelcontextprotocol.io/v0.1/servers?search=replicant-mcp" | jq -e '.servers[0].server.name' && curl -sL "https://glama.ai/mcp/servers/thecombatwombat/replicant-mcp" -o /dev/null -w "%{http_code}" | grep -q "200" && echo "PASS: VER-01 and VER-03 automated checks succeeded"</automated>
  </verify>
  <done>VER-01 (MCP Registry) and VER-03 (Glama) have automated PASS/FAIL results. VER-05, VER-06, VER-07 have document-based status. REQUIREMENTS.md updated with all statuses except VER-02 and VER-04 (pending human check).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies Smithery listing and awesome-mcp-servers PR status</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/04-verification/04-01-SUMMARY.md</files>
  <action>
Present the user with browser verification instructions for VER-02 and VER-04. After receiving user feedback, update REQUIREMENTS.md with final VER-02 and VER-04 status, then create the phase SUMMARY documenting all 7 verification results.

**What was built:** Automated verification completed for 5 of 7 VER requirements. Two require browser-based confirmation because their APIs are inaccessible (Smithery returns 429; awesome-mcp-servers GitHub API returns 404).

**How to verify:**

VER-02 -- Smithery listing:
1. Open https://smithery.ai/server/@replicant-co/replicant-mcp in your browser
2. Confirm the page loads with replicant-mcp listing (description, install option)
3. If that URL 404s, search "replicant-mcp" at https://smithery.ai
4. Report: "Smithery PASS" if listing found, or "Smithery NOT FOUND" if not visible

VER-04 -- awesome-mcp-servers:
1. Open https://github.com/punkpeye/awesome-mcp-servers/pull/3919 in your browser
2. Check if the PR is merged, open, or closed
3. If merged, also check https://mcpservers.org and search for "replicant-mcp"
4. Report: "awesome-mcp MERGED" + "mcpservers.org FOUND/NOT FOUND", OR "awesome-mcp OPEN" if PR still pending, OR "awesome-mcp CLOSED" if rejected

**Resume signal:** Provide status for both: Smithery (PASS/NOT FOUND) and awesome-mcp-servers (MERGED/OPEN/CLOSED + mcpservers.org status if merged)

After receiving user input, update REQUIREMENTS.md with final VER-02 and VER-04 status (mark complete, pending external, or note rejection). Then create the 04-01-SUMMARY.md with the complete verification results table for all 7 VER requirements.
  </action>
  <verify>All 7 VER requirements in REQUIREMENTS.md have a final status (checked, deferred, or annotated). 04-01-SUMMARY.md exists with full verification table.</verify>
  <done>REQUIREMENTS.md fully updated. Phase SUMMARY created with all 7 marketplace verification results documented.</done>
</task>

</tasks>

<verification>
All 7 VER requirements have a resolution:
- VER-01: Automated API check (PASS/FAIL)
- VER-02: Browser check by user (PASS/NOT FOUND)
- VER-03: Automated HTTP check (PASS/FAIL)
- VER-04: Browser check by user (MERGED/OPEN/CLOSED)
- VER-05: Document evidence (CONFIRMED SUBMITTED)
- VER-06: N/A (deferred per FORM-02)
- VER-07: Document evidence (CONFIRMED SUBMITTED)

REQUIREMENTS.md reflects final status for all items.
</verification>

<success_criteria>
1. Every VER requirement has a definitive status (PASS, CONFIRMED SUBMITTED, PENDING EXTERNAL, N/A, or FAIL)
2. REQUIREMENTS.md traceability table is fully updated
3. No VER item is left as "pending" without explanation
4. VER-06 correctly reflects FORM-02 deferral
5. Phase SUMMARY documents the complete marketplace distribution outcome
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification/04-01-SUMMARY.md`
</output>
